<% if current_user %>
  <%= search_form_for(@q, url: search_path, method: :get, class:'d-flex') do |f| %>
    <%= f.search_field :title_or_body_or_user_name_i_cont_any %>
    <%= f.submit "Search!", class:"btn btn-outline-success" %>
  <% end %>
<% end %>
